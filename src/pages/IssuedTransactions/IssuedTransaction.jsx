import { Button, Input } from "@chakra-ui/react";
import React from "react";

const IssuedTransaction = () => {
  return (
    <div>
      <Button>Issue A Book</Button>
      <Input />
    </div>
  );
};

export default IssuedTransaction;
